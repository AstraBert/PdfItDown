[project]
name = "pdfitdown-docs"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.10,<3.14"
dependencies = [
    "pdfitdown[server]",
]

[tool.uv.sources]
pdfitdown = {workspace = true}